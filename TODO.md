# TwinDesk TODO

This is the execution tracker for the roadmap in `docs/ROADMAP.md`. Product scope and safety boundaries remain authoritative in `docs/PRODUCT_GOALS.md`; architecture constraints remain authoritative in `docs/ARCHITECTURE.md`.

## Status Rules

- `[ ]` not started
- `[x]` completed and verified
- Add `Status: In progress` below an unchecked task while it is actively being worked on.
- Move intentionally deferred or removed work to **Explicitly Deferred** with a reason.

Update this file in the same change that completes, defers, or materially re-scopes a task. Do not mark a task complete when only scaffolding exists; its stated completion checks must pass.

## Project Foundation

- [x] **TD-001 — Initialize the TwinDesk repository**
  - Repository uses `main` and tracks `origin/main`.
  - Initial documentation is committed with DCO sign-off.
- [x] **TD-002 — Define product goals and boundaries**
  - Product vision, personas, Skills, approval levels, connector limitations, MVP scope, non-goals, and acceptance criteria are documented.
- [x] **TD-003 — Define the initial architecture and roadmap**
  - Plugin boundaries, persistence boundaries, security model, Harness strategy, and staged delivery plan are documented.
- [x] **TD-004 — Add repository-wide Agent instructions**
  - Root `AGENTS.md` defines safety, architecture, testing, documentation, and Git rules.
  - Repository documentation is required to be written in English.

## Completed Milestone: Stage 0 — Harness Compatibility Validation

Goal: prove that TwinDesk can be built primarily as out-of-tree DeepSeek Harness plugins before product implementation begins.

### Baseline and Workspace

- [x] **TD-010 — Select and pin the current latest Harness release**
  - Record the exact release or commit and its license.
  - Record the required Node and package-manager versions.
  - Document how the dependency is obtained locally and in CI.
  - Resolve `latest` only during an intentional upgrade, then commit an exact package version and Git revision.
  - Verification record: `docs/HARNESS_VERSION.md` pins `@deepseek-ai/dsh@0.1.1-rc.2` and commit `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`.
  - Completion check: a clean environment resolves the same Harness revision without following a floating branch.

- [x] **TD-011 — Scaffold the TwinDesk monorepo**
  - Create a pnpm workspace and TypeScript project references.
  - Add initial package directories without implementing product behavior.
  - Add formatting, type-check, unit-test, and repository-check commands.
  - Add an `.env.example` containing names only, never real credentials.
  - Depends on: TD-010.
  - Verification record: the frozen install and combined `pnpm run check` pass from a clean temporary copy.
  - Completion check: install, type-check, test, and build commands pass from a clean checkout.

- [x] **TD-012 — Define the Harness adapter boundary**
  - Create an adapter package that owns Harness and Cordis imports.
  - Keep `@twindesk/domain` free of Harness, Cordis, Feishu, Jira, UI, and model SDK dependencies.
  - Add a dependency-boundary test or static check.
  - Depends on: TD-011.
  - Verification record: static import/dependency checks, built declaration checks, and a runtime import-denial test protect the boundary.
  - Completion check: domain tests run without loading Harness packages.

### Server-Side Plugin Validation

- [x] **TD-020 — Create a minimal Profile Bundle**
  - Compose the pinned Harness base and Web application bundles.
  - Add one TwinDesk Host plugin through the Profile rather than a core patch.
  - Document local launch and configuration inspection commands.
  - Depends on: TD-011.
  - Verification record: a clean temporary copy installs the local Bundle and Host plugin through the Profile, dumps the expected entry, starts the Web surface on a temporary loopback port, and shuts down normally.
  - Completion check: the Profile starts from a clean checkout and appears in the dumped effective configuration.

- [x] **TD-021 — Register a read-only TwinDesk Tool**
  - Implement a synthetic `twindesk_status` or equivalent Tool in an out-of-tree Host plugin.
  - Return structured, deterministic data with no network or filesystem writes.
  - Verify Tool registration, invocation, cancellation, and plugin disposal.
  - Depends on: TD-020.
  - Verification record: a keyless scripted Agent receives `twindesk_status`, records a successful `tool/call` and `tool/result` pair in its Session, and the same composition verifies structured output, pre-dispatch cancellation, and registry cleanup on plugin disposal.
  - Completion check: an Agent invokes the Tool successfully and the invocation appears in the Session trace.

- [x] **TD-022 — Validate plugin-owned settings**
  - Register one non-secret settings namespace and schema.
  - Confirm values survive restart.
  - Confirm secret fields are excluded or redacted at every browser and diagnostic boundary.
  - Depends on: TD-020.
  - Verification record: the `twindesk-work-hub` namespace updates `includeRoadmapStage` live, persists through the file-backed provider, and recovers in a fresh Context; an undeclared synthetic secret-like field is rejected before persistence and is absent from the redacted browser descriptor, rejection diagnostic, and stored YAML.
  - Completion check: settings can be read and updated through the supported plugin contract without modifying Harness core.

### Client Plugin and Inbox Extension Validation

- [x] **TD-030 — Build and load a Client plugin**
  - Produce the required `dsh.client` bundle format outside the Harness repository.
  - Mount a small TwinDesk settings card or diagnostic component.
  - Verify production build, source maps, loading failure diagnostics, and reload behavior.
  - Depends on: TD-020.
  - Verification record: the installed `@twindesk/plugin-ui` package registers a Work Hub diagnostic card from an external lazy-CJS bundle; tests execute and dispose the production factory across reload, reject missing or malformed artifacts with actionable diagnostics, and fetch a stable boot-graph row plus the bundle and embedded-source map from a live Profile.
  - Completion check: the component loads from the installed Profile in a clean production build.

- [x] **TD-031 — Spike a top-level Inbox surface**
  - Attempt to add an Inbox route, sidebar entry, and empty-state page using supported Client extension points.
  - Do not add Work Hub business logic during this spike.
  - Record every internal or unstable API required.
  - Depends on: TD-030.
  - Verification record: the external Client plugin owns a `#/inbox` deep link, contributes a `sidebar.footer.action`, and temporarily shadows the top-level `conversation` slot with a static empty page; the pinned SlotCore probe and production bundle lifecycle test verify shadow, restore, direct-route load, listener cleanup, disposal, and reload behavior, while `docs/INBOX_EXTENSION_SPIKE.md` records the absence of a public Router and primary navigation list.
  - Completion check: either the route works entirely out of tree, or a written gap report identifies the smallest generic Harness UI Slot required.

- [x] **TD-032 — Decide the core-patch policy for the Inbox**
  - Choose one of: no core change, upstream a generic extension point, or maintain a minimal temporary patch.
  - Record the decision as an ADR with upgrade and ownership consequences.
  - Depends on: TD-031.
  - Verification record: ADR 0001 recorded the product-neutral upstream option and rejected a Harness fork or temporary patch. ADR 0002 later followed its explicit reassessment path, selected a standalone TwinDesk-owned product shell, and retained the Harness Client spike as diagnostics only.
  - Completion check: the chosen path keeps TwinDesk domain logic outside Harness core.

### Personas, Persistence, and Delegation

- [x] **TD-040 — Create two distinct Agent Presets**
  - Create a technical-lead Persona and a communication Persona.
  - Give them visibly different instructions and Tool/Skill exposure.
  - Keep external writes disabled.
  - Depends on: TD-020, TD-021.
  - Verification record: the versioned technical-lead and communication Presets compose through the pinned public Harness Loader and Agent Preset services; a keyless deterministic probe gives both the same synthetic release-delay request and verifies distinct prompts, exact scoped Skill and read-only Tool exposure, distinct draft-only responses, preset identity, and independent disposal. Profile preparation copies only missing Presets into the supported Harness-home root, validates matching copies byte-for-byte, and refuses to overwrite divergent content.
  - Completion check: the same fixture request produces behavior consistent with each Persona's configuration and authority.

- [x] **TD-041 — Validate Session persistence and restart recovery**
  - Select JSONL or SQLite explicitly for the spike and document why.
  - Persist a Session containing messages, a Tool call, and Persona identity.
  - Restart the Host and resume the Session.
  - Depends on: TD-020, TD-040.
  - Verification record: the pinned JSONL backend durably stores a synthetic technical-lead Session and preserves it across two cold Host restarts using the Profile's default Zstandard encoding and chunk packing; a separate raw-encoding case repairs an injected incomplete tail. Both restore the recorded Preset before mounting its scoped Persona, Skill, and Tools, preserve identical messages and Tool trace, and use contiguous event projections to prove the first resume adds only its required `session/end-seed` while the second adds nothing.
  - Completion check: the resumed Session preserves history, Persona identity, and Tool trace without duplicate events.

- [x] **TD-042 — Validate Codex as a specialist Subagent**
  - Install and configure `subagent-codex` through the Profile.
  - Delegate one bounded, read-only repository task.
  - Verify cancellation, depth limits, Tool filtering, and result attribution.
  - Depends on: TD-020.
  - Verification record: the Profile installs the exact Harness Codex provider and binds only the technical Preset to a foreground `subagent_codex` Tool backed by an isolated native `approval_policy = "never"`, `sandbox_mode = "read-only"` Codex home. An adapter-owned loopback fixture drives the real package-local Codex 0.147.0 process through a repository read, a sandbox-denied write with no file effect, and cancellation to quiescence. Paired lifecycle events and the Lead Session Tool trace attribute the result. This provider advertises no Harness depth-limit or Tool-filter capability, so numeric depth, per-run Tool filters, and a numerically capped Tool mount are tested to fail before any child request; `provider-managed` depth is recorded as a Stage 4 limitation rather than a silent guarantee.
  - Completion check: the Lead receives a traceable result while the child cannot exceed the delegated authority.

### Compatibility and Stage Exit

- [x] **TD-050 — Build the Harness compatibility smoke suite**
  - Cover Profile boot, Host plugin activation, Tool registration, Client plugin loading, Persona selection, Session resume, and Codex delegation.
  - Pin expected public interfaces and fail clearly on incompatible upgrades.
  - Depends on: TD-021, TD-030, TD-040, TD-041, TD-042.
  - Verification record: `pnpm compat:check` builds production artifacts, runs an explicit integrity-tested manifest covering the pinned adapter contracts, Host Tool and Settings activation, production Client loading and Inbox lifecycle, Profile and Preset composition, Persona selection, duplicate-free Session recovery, and real package-local Codex delegation. It then validates the built adapter boundary and boots the effective Profile on loopback. Each stage reports its capability, status or signal, and exact reproduction command; unsupported package versions and capabilities continue to fail before behavioral success can be inferred.
  - Completion check: one command runs all compatibility checks in CI and locally.

- [x] **TD-051 — Publish the Stage 0 compatibility report**
  - Record the validated Harness revision and supported extension points.
  - List gaps, unstable APIs, required patches, and upgrade risks.
  - Estimate only the implementation surface, not speculative delivery dates.
  - Depends on: TD-032, TD-050.
  - Verification record: `docs/STAGE_0_COMPATIBILITY_REPORT.md` records the exact Harness tag and commit, published package and toolchain pins, validated public extension points, compatibility-only seams, gaps, Stage 1 and later implementation surfaces, upgrade risks, and security boundaries. After ADR 0002 removed Harness Client navigation from the product path, the report recommends **GO** for fixture-driven Stage 1 while approving no Harness fork, TwinDesk-specific core patch, real Connector write, or weakened safety boundary.
  - Completion check: the report makes a clear go/no-go recommendation for Stage 1.

- [x] **TD-052 — Pass the Stage 0 exit gate**
  - Product experience is viable without a fork, or with only a minimal generic UI extension point.
  - No TwinDesk domain logic is placed in Harness core.
  - Compatibility tests cover every selected unstable boundary.
  - All Stage 0 security and restart checks pass.
  - Depends on: TD-051.
  - Verification record: [ADR 0002](docs/decisions/0002-twindesk-owned-product-web-shell.md) selects a TwinDesk-owned local Web shell and keeps Harness as a replaceable runtime and diagnostic UI. `@twindesk/web` owns deterministic Inbox, Personas, Connectors, Audit, and Settings routes, rejects non-loopback binding, serves restrictive security headers, shuts down explicitly, and restarts on the same port. The full repository and compatibility checks pass with no Harness fork, core patch, or TwinDesk domain logic in Harness.
  - Gate audit (2026-08-26): **PASSED**. All four criteria pass in `docs/STAGE_0_EXIT_GATE.md`. The optional upstream navigation proposal is retained as ecosystem reference and no longer blocks product delivery.

## Current Milestone: Stage 1 — Local Work Hub

TD-052 is complete. Stage 1 may now build the fixture-driven local loop before
connecting a real account or enabling external writes.

- [x] **TD-100 — Define versioned domain types** for ExternalEvent, WorkItem, ExternalThread, Draft, ActionProposal, ApprovalRecord, ConnectorCursor, and AuditRecord.
  - Verification record: `@twindesk/domain` exports schema-versioned, discriminated, deeply immutable records and fail-closed parsers for all eight types. Boundary tests cover unsupported versions and fields, stable and duplicate references, event chronology, explicit partial context, finite normalized JSON, Persona selection, action identity/target matching, content digests, idempotency keys, approval expiry and one-time consumption, attributable audit actors, accessor rejection, and diagnostics that do not echo rejected values. Domain isolation remains intact.
- [x] **TD-101 — Define the Connector contract** with lifecycle, synchronization, context, proposal, execution, and health semantics.
  - Verification record: `@twindesk/domain` exports Connector contract version 1 with an exact descriptor, idempotent start/stop lifecycle, cancellation on every operation, candidate-cursor synchronization, explicit complete/partial/unavailable context, preview-only proposal creation, opaque approved execution input, success/failure/uncertain receipts, per-identity scope health, typed payload-free operational errors, and fail-closed runtime registration validation. Synthetic contract tests cover lifecycle restart safety, cancellation, partial context, malformed versions/capabilities/methods, and actionable error fields.
- [x] **TD-102 — Design the TwinDesk SQLite schema and forward migrations** without using Harness Session tables.
  - Verification record: `@twindesk/storage-sqlite` uses the Node.js 24 built-in SQLite driver with a TwinDesk-specific application ID, strict version 1 business tables, checksummed migration history, transactional forward-only migrations, WAL and full durability for file databases, an intentionally narrow lifecycle handle, and fail-closed rejection of unowned, future, or tampered databases. Tests cover clean initialization, restart data recovery without duplicate migrations, Harness-like database isolation, immutable event rows, migration rollback, future-version rejection, history tampering, and safe open options. `docs/STORAGE_SQLITE.md` records table ownership plus privacy and retention review.
- [x] **TD-103 — Implement idempotent event ingestion** for duplicates, out-of-order events, and replay.
  - Verification record: `TwinDeskDatabase.ingestExternalEvents()` validates complete batches before a serialized `BEGIN IMMEDIATE` write, appends new normalized events, recognizes semantic duplicates within a batch and after restart, preserves the first local receive time during replay, accepts source-time out-of-order delivery, and rolls back the whole batch when a stable ID or idempotency key maps to different immutable content. Typed conflict and storage errors expose only the input index and conflict-key category, never content or identifiers. Synthetic tests cover duplicate replay, reordered JSON, later receive times, out-of-order events, ID/key conflicts, interrupted batches, runtime validation, restart recovery, and closed handles. `docs/EVENT_INGESTION.md` defines the standalone contract and links the TD-104 atomic cursor path.
- [x] **TD-104 — Implement durable synchronization cursors** that advance only after event commits.
  - Verification record: `TwinDeskDatabase.commitConnectorSyncBatch()` validates the Connector, account, and stream boundary before opening a serialized transaction, ingests every event, and inserts or advances the candidate cursor in the same commit. `getConnectorCursor()` restores the durable checkpoint after restart. Exact checkpoint replay is unchanged, an empty event batch may advance a cursor, and an absent candidate leaves it untouched. Stable identity conflicts, timestamp or source-watermark regressions, event conflicts, and interrupted cursor writes fail closed and roll back the whole batch. Synthetic tests cover restart recovery, replay, event-only commits, identity and accessor rejection, cursor identity conflicts, interrupted writes, watermark removal and regression, closed handles, and payload-free errors. [Durable Synchronization Cursors](docs/SYNC_CURSORS.md) records the opaque-position limitation.
- [x] **TD-105 — Build Work Item projections and Inbox queries** that are rebuildable from events and user actions.
  - Verification record: `TwinDeskDatabase.putWorkItemProjection()` validates versioned Thread and Work Item records against durable source events, preserves append-only source associations and source timestamps, stores an event-anchored projection base, and materializes the current Work Item atomically. Revisioned `WorkItemUserAction` records apply Inbox-state and Persona choices without granting authority; exact replay is idempotent, identity/revision conflicts fail closed, and a newer event-derived base supersedes older actions. `rebuildWorkItemProjection()` restores damaged derived rows from the base and immutable actions across restart. `queryInbox()` provides strict state filters and chronological keyset pagination that remains correct across optional timestamp precision. Migration 2 transactionally backfills existing version 1 Work Items without deletion. Synthetic tests cover restart, replay with reordered fields, source and timestamp mismatch, missing and out-of-order actions, action conflicts, newer events, interrupted projection writes, migration, rebuild, filtering, pagination, closed handles, and payload-free errors. [Work Item Projections](docs/WORK_ITEM_PROJECTIONS.md) records the routing and privacy limitations.
- [x] **TD-106 — Build the fixture-driven Inbox page** with Needs reply, Needs review, Waiting, and Done states.
  - Verification record: `@twindesk/plugin-work-hub` idempotently seeds four synthetic ExternalEvent, Thread, and Work Item records through the durable SQLite ingestion and projection boundaries, then exposes a presentation-safe fixture read model without account IDs, external object IDs, or raw normalized payloads. The loopback-only Web server provides a strict read-only `/api/inbox` endpoint with per-state filtering and all-state counts. The simple product Inbox loads each tab, renders a selectable list and detail pane, distinguishes complete and partial context, displays the current persisted Persona identity without granting authority, validates the full versioned API response before rendering, escapes API-derived strings, and states that no external write is possible. File-backed tests verify duplicate-free restart recovery, all four state filters, changed Persona identity, explicit shutdown, invalid query rejection, HEAD and method behavior, malformed response rejection, and browser/API integration. [Fixture-driven Inbox](docs/FIXTURE_INBOX.md) records the data flow, privacy boundary, startup path, and limitations.
- [x] **TD-107 — Map Persona configuration to Harness Presets** without granting permissions implicitly.
  - Verification record: `@twindesk/plugin-work-hub/persona-presets` defines two immutable version 1 built-in Persona configurations and fail-closed mappings to the pinned `twindesk-technical-lead` and `twindesk-communication` Agent Presets. Exact shape and installed-content checks reject accessors, symbols, unknown Personas, unsupported profiles, higher autonomy, changed behavior, and injected Tool, permission, credential, or Connector-scope fields without echoing rejected data. Each mapping exposes the expected Skill and model-facing Tool names while explicitly recording `authorityEffect: none`, `externalWritesAvailable: false`, and `draft_only`; the Inbox now derives Persona display names from the same configuration source. The real Harness composition probe consumes the mapped Preset IDs and verifies its actual Skills and Tools match exactly, including the already bounded read-only Codex specialist and the absence of send, write, delete, shell, or bash capabilities. [Persona to Harness Preset Mapping](docs/PERSONA_PRESET_MAPPING.md) records the separation from Policy, credentials, Connector scopes, approvals, and future dynamic Persona materialization.
- [x] **TD-108 — Implement Draft and ActionProposal state transitions** with no external side effects.
  - Verification record: `@twindesk/domain` defines fail-closed local transition records and pure state functions, while `TwinDeskDatabase` durably creates, reads, and advances sequential Persona-bound Drafts and exact-content ActionProposals. Migration 3 preserves versioned immutable creation snapshots and transition histories so exact retries remain idempotent after state changes and restart. Draft transitions stop at review, supersede, or cancellation; proposal transitions stop at awaiting approval, rejection, or cancellation, and cannot enter approval or execution states without future evidence. Synthetic tests cover replay, restart recovery, revision and active-Draft rules, identity/target/content/digest/chronology conflicts, accessor rejection, interrupted-write rollback, closed handles, payload-free errors, and the absence of approval records or external receipts. [Draft and ActionProposal Transitions](docs/DRAFT_ACTION_TRANSITIONS.md) records the state graphs and remaining boundaries.
- [x] **TD-109 — Implement the local Audit Timeline** linking Work Items, Sessions, Runs, Tool calls, approvals, and receipts.
  - Verification record: `TwinDeskDatabase.appendAuditRecords()` validates and atomically appends immutable version 1 business records, recognizes exact replay, rejects identity conflicts, and fails closed for missing, mismatched, or chronologically impossible local references. Work Item-owned Draft, ActionProposal, ApprovalRecord, and ActionReceipt references are resolved inside TwinDesk SQLite; Session, Run, and Tool-call identifiers remain opaque cross-store links and must form a bounded Work Item → Session → Run → Tool-call chain. Strict Work Item, reference, category, and outcome filters use stable chronological keyset pagination and recover across restart. The loopback-only `/api/audit` endpoint returns a versioned presentation-safe projection without record IDs, actor IDs, details, or content, and the Audit page renders four synthetic routing records. Tests cover replay, restart, pagination, reference mismatch, chronology, immutable records, interrupted writes, malformed browser data, redacted diagnostics, HTTP methods, and store separation. [Local Audit Timeline](docs/AUDIT_TIMELINE.md) records the persistence and privacy boundaries.
- [x] **TD-110 — Implement secret references and a shared redactor** for logs, model context, errors, telemetry, and exports.
  - Verification record: `@twindesk/domain` defines a strict, deeply immutable version 1 `SecretReference` containing only a namespaced opaque locator, secret-store kind, and purpose; it accepts no secret value and grants no Connector scope or authority. `redactForBoundary()` provides explicit `logs`, `errors`, `telemetry`, `model_context`, and `exports` policies. All policies remove credential fields, SecretReference locators, supplied in-memory secret values, recognizable inline credentials, environment containers, caller-declared sensitive fields, and hidden reasoning. Diagnostic policies additionally retain only allowlisted structured metadata text and redact business content; model context and export may retain authorized business text. Results include frozen count-only summaries with no paths or values, while accessors, symbols, cycles, non-JSON values, excessive depth/size, and hostile Proxies fail closed. Both current Work Hub Tool renderers now pass values through the shared model-context serializer before Harness sees them. Synthetic tests cover API keys, OAuth tokens, cookies, PEM keys, environment values, hidden reasoning, overlapping boundary policies, immutable input, error serialization, malformed options, accessor avoidance, cycles, Proxy traps, and the real Work Hub model-context boundary without real credentials. [Secret References and Shared Redaction](docs/SECRET_REFERENCES_AND_REDACTION.md) records the threat model and the remaining Keychain, export, and retention work.
- [x] **TD-111 — Implement Thread export and deletion** with explicit retention behavior.
  - Verification record: `TwinDeskDatabase.exportThread()` returns a consistent, versioned aggregate covering the selected Thread, normalized source events, current and base Work Item projections, revisioned user actions, Draft/ActionProposal creation and transition history, approvals, receipts, and related business Audit records. Every document passes through the shared export redactor; exact in-memory known secrets can be supplied without persistence, and cross-Thread Audit records fail closed. `deleteThread()` requires a versioned idempotency identity, exact Thread revision, and request time; one immediate transaction deletes Thread-owned projections and histories, related Audit records, and only source events that become orphaned. Shared events, Connector cursors, other Threads, and the separate Harness Session store are retained explicitly. Migration 5 adds immutable hash-and-count-only deletion receipts, exact retries recover after restart, different retries report an already deleted Thread, and tombstones prevent silent projection resurrection. Tests cover redacted export, all persisted record classes, restart, stale revisions, hostile accessors, shared events, cursor retention, interrupted rollback, closed handles, forward migration, and absence of raw identity, content, credentials, or Harness IDs in receipts. [Thread Export and Deletion](docs/THREAD_EXPORT_AND_DELETION.md) records the authorization, transaction, retention, and cross-store boundaries.
- [x] **TD-112 — Pass the Stage 1 exit gate:** fixture events complete Inbox → Persona → Draft → Audit across restart.
  - Verification record: the versioned fixture service idempotently completes two exact built-in Persona mappings into deterministic `ready_for_review` Drafts and immutable Persona-attributed Audit records linked to their Work Items, source events, and Drafts. The flow preserves explicit partial context, reports `draft_only` with no authority or external-write capability, and creates no ActionProposal, approval, receipt, model call, Session, Run, Connector execution, or external effect. File-backed tests verify identical projections across restart, all-Persona preflight before writes, fail-closed explicit Persona changes, interrupted Draft and Audit writes, and idempotent repair without duplicate Drafts. The product Audit page exposes the two Draft records with the four routing records through its presentation-safe loopback API. [Stage 1 Exit Gate](docs/STAGE_1_EXIT_GATE.md) records the formal decision and remaining Stage 2 boundary.

## Stage 2 Backlog — Feishu Closed-Loop MVP

Do not start write operations before the approval and audit path exists.

- [x] **TD-200 — Define separate Feishu Bot and User identities** and store only credential references.
  - Verification record: `@twindesk/plugin-feishu` defines a strict version 1 configuration with separate Bot and User principal slots under one Feishu application. Bot credentials require a `connector_app_credential` SecretReference, User credentials require a distinct `connector_oauth` SecretReference, and the credential-free ActionIdentity projection grants no scope or authority. The file-backed store validates before writing, atomically replaces a `0600` JSON document, reads with `O_NOFOLLOW`, rejects symlinks, non-regular and oversized files, and emits payload-free typed failures. Tests cover restart recovery, separate identities, mixed-purpose and shared-locator rejection, hostile accessors, undeclared credential fields, rejected-write rollback, and the absence of app secrets, access tokens, refresh tokens, cookies, and private keys in persistence. [Feishu Bot and User Identities](docs/FEISHU_IDENTITIES.md) records visibility, privacy, retention, revocation, and remaining Keychain and scope limitations.
- [x] **TD-201 — Consume Bot direct-message and mention events** with signature validation and deduplication.
  - Verification record: `@twindesk/plugin-feishu` now verifies the timestamp, nonce, five-minute replay window, application and tenant identity, and timing-safe SHA-256 signature over untouched callback bytes before parsing. It supports signed plaintext and AES-256-CBC encrypted version 2 `im.message.receive_v1` payloads, accepts Bot direct messages and exact Bot mentions only, and preserves the delivery ID without using it for idempotency. `message_id` drives serialized semantic deduplication; the downstream handler completes before an append-and-fsync receipt, handler failures remain retryable, concurrent duplicates invoke once, and changed reuse fails closed. The `0600`, `O_NOFOLLOW`, 32 MiB JSONL journal survives restart and torn tails while retaining only versioned message/event hashes and verified receive times—never raw payloads, external IDs, principals, content, credentials, or the Encrypt Key. Synthetic tests cover invalid/stale signatures, identity mismatch, encrypted callbacks, duplicate/out-of-order delivery, restart, interruption, downstream failure, hostile accessors, unsafe files, and persistence redaction. [Feishu Bot Event Ingestion](docs/FEISHU_BOT_EVENT_INGESTION.md) records the Bot-only visibility, commit order, privacy, retention, hosting, and TD-204 idempotency boundaries.
- [x] **TD-202 — Incrementally discover messages visible under User identity** without claiming complete account coverage.
  - Verification record: `@twindesk/plugin-feishu` defines a User-only search client contract and a bounded incremental discoverer for the `user_visible_messages` stream. Exact account, app, tenant, principal, and `identityType: user` checks prevent Bot/User substitution. The first run searches a configurable lookback, later runs overlap the last fully exhausted watermark, and a fixed indexing delay avoids claiming the newest unindexed interval. One page is returned per call with an opaque candidate `ConnectorCursor`; active pagination retains the prior `committedThrough`, the final page advances it, and expired page tokens restart the same window for idempotent replay. Missing message details remain explicit, count-only issues and suppress cursor advancement instead of becoming empty success. Typed authorization, scope, rate-limit, network, cancellation, malformed-response, hostile-value, and cursor failures expose no payloads. Every batch reports partial authorized-search coverage, stores nothing itself, and performs no write; TD-204 consumes it through the atomic ExternalEvent/projection/cursor boundary. Synthetic tests cover pagination restart, rolling overlap, out-of-order messages, missing details, token expiry, identity/cursor conflicts, cancellation, immutability, and caught-up polling. [Feishu User Message Discovery](docs/FEISHU_USER_MESSAGE_DISCOVERY.md) records scopes, visibility, privacy, cursor, replay, and remaining adapter limitations.
- [x] **TD-203 — Retrieve bounded conversation, document, and attachment context** with explicit partial-result states.
  - Verification record: `@twindesk/plugin-feishu` now exposes a User-only context client contract and `FeishuContextRetriever` returning product-owned `ConnectorContextBundle` values. Requests bind the exact account, application, tenant, User principal, anchor reference, purpose, item limit, and optional conversation time bound. Conversation reads are descending and reaction-free; referenced document excerpts and attachment text have configurable hard bounds; binary attachment values are never accepted. Responses must preserve the same identity and anchor and may contain only normalized message text, simple document excerpts with revision/truncation state, or attachment text excerpts/metadata-only reasons. Successful plus failed source counts share the same item budget, and replies require their exact thread. `complete` cannot be empty or hide pagination, truncation, metadata-only bodies, or source failures; `partial` retains valid items with explicit missing categories and retryable conversation/document/attachment failures; `unavailable` requires a concrete source problem. Typed authorization, scope, rate-limit, network, cancellation, malformed-response, hostile-array, chronology, duplicate, oversized, and raw-binary failures expose no rejected payload. The retriever persists nothing and performs no external write. Synthetic tests cover all three sources, fixed/custom bounds, complete/partial/unavailable states, time bounds, partial resource failures, identity/anchor mismatches, sparse/accessor data, cancellation, and payload-free client errors. [Feishu Context Retrieval](docs/FEISHU_CONTEXT_RETRIEVAL.md) records visibility, scopes, privacy, retention, and remaining adapter limitations.
- [x] **TD-204 — Normalize Feishu sources into ExternalEvents and Work Items.**
  - Verification record: `@twindesk/plugin-feishu` canonicalizes verified Bot messages and bounded User discovery batches into version 1 ExternalEvents plus conversation-scoped ExternalThread and Work Item projections. Stable account/message/state hashes make Bot/User observations converge while edits and deletions remain immutable events; inconsistent canonical bodies fail closed. Direct messages and exact configured-principal mentions route to `needs_reply`, other discovered groups to `needs_review`, and latest deletions to `done`, without granting Persona or Connector authority. Late source states extend history without regressing Inbox presentation, and existing Persona selections survive projection updates. `commitConnectorSyncBatch()` now validates and atomically commits events, bounded projection batches, and optional candidate cursors, so restart cannot advance past missing Inbox state. Normalization persists no raw callback/search payload, delivery metadata, credentials, or sender display names and marks unretrieved context explicitly partial. Synthetic tests cover cross-path replay, restart, update and out-of-order deletion routing, identity mismatch redaction, and injected rollback across event, projection, and cursor state. [Feishu Message Normalization](docs/FEISHU_MESSAGE_NORMALIZATION.md) records canonicalization, routing, transaction, privacy, and remaining runtime limitations.
- [x] **TD-205 — Implement a preview-only Feishu reply proposal.**
  - Verification record: `@twindesk/plugin-feishu` now implements the side-effect-free Feishu `propose` boundary. `FeishuReplyProposer` accepts only `feishu.reply`, requires an existing Draft identity, binds the exact Work Item, configured Bot or User ActionIdentity, current timestamped Feishu message target, bounded plain-text content, canonical content digest, and opaque hash-only proposal/idempotency identities, and returns a version 1 `ActionProposal` in `proposed` state with `risk: write`. Persisting through the existing SQLite boundary additionally requires a `ready_for_review`, same-Work-Item, content-identical Draft and a target reference on the durable Thread. The proposer has no Feishu client or credential input and cannot create an approval transition, ApprovalRecord, approved capability, execution attempt, ActionReceipt, or external effect. Synthetic tests cover message normalization through durable proposal restart recovery, explicit Bot/User separation, exact Draft/content/target binding, absence of approval and receipt rows, cancellation, missing Draft, unsupported action/media, identity spoofing, hostile accessors, and payload-free failures. [Feishu Reply Proposal](docs/FEISHU_REPLY_PROPOSAL.md) records state separation, identity, target, idempotency, privacy, and remaining UI/approval/execution work.
- [x] **TD-206 — Bind one-time approval to sending identity, target, content digest, and expiration.**
  - Verification record: `@twindesk/storage-sqlite` now implements a Connector-neutral version 1 approval policy over durable ActionProposals. `requestActionApproval()` computes canonical SHA-256 bindings for the exact Connector/account/Bot-or-User/display identity, timestamped external target, and proposal content; requires an explicit expiration of at most 24 hours; compares all lifecycle timestamps with a trusted local clock; and atomically inserts the pending ApprovalRecord, records `proposed -> awaiting_approval`, and updates the proposal. `decideActionApproval()` requires the same three digests and a responder for approval, rejection, or cancellation; automatic expiration has no responder; and the ApprovalRecord plus proposal advance atomically to approved, rejected, or cancelled evidence states. `consumeActionApproval()` fails closed unless the approval is approved, unexpired in both supplied and observed time, unconsumed, and still exactly bound, then writes `consumedAt` once before returning an opaque ApprovedAction. Consumption replay can recover only the same hash-derived execution-attempt identity and proposal idempotency key before observed expiration; it grants no second authorization and performs no Connector call. Approval records remain covered by Thread export/deletion and Audit reference validation. Synthetic Feishu-flow tests cover request and consumption restart recovery, exact bindings, approval/rejection/cancellation/expiration, missing responders, expired and backdated decisions or consumption, binding tampering, hostile accessors, interrupted atomic decisions, closed handles, zero receipts, and payload-free errors. [One-Time Action Approval Policy](docs/ACTION_APPROVAL_POLICY.md) records UI display requirements, policy limits, one-time recovery, privacy, audit, and remaining TD-207 scope/receipt checks.
- [x] **TD-207 — Implement idempotent Feishu reply execution** and uncertain-result handling.
  - Verification record: `@twindesk/plugin-feishu` now validates the exact opaque ApprovedAction, configured Bot/User identity, timestamped message target, content and approval digests, expiration, and stable execution-attempt/idempotency identities before an external write. A salted hash in the proposal key additionally binds the configured app, principal, and SecretReference metadata so identity-slot or credential rotation invalidates the old approval without persisting those values in the proposal. Its injected credential-resolving client boundary receives only the configured SecretReference and exact normalized request. `execute()` uses exact reconciliation first when the client supports it; a send-only client can make only its first durably reserved dispatch and never treats unsupported lookup as remote absence. Known rate limiting and proven preflight unavailability are retryable only with the same key; missing authorization/scope or explicit rejection is terminal; network, unknown, identity-inconsistent, and malformed post-send results become payload-free `uncertain` receipts requiring reconciliation or operator recovery. `@twindesk/storage-sqlite` atomically starts durable execution and records or advances one receipt projection with proposal state, supports restart recovery of consumed non-terminal attempts, refuses terminal replay and expired new sends, and permits supported reconciliation after expiration without granting send authority. Synthetic closed-loop tests cover success, exact and unsupported reconciliation, send-only first dispatch and restart blocking, exact one-send idempotency, restart and expired reconciliation, preflight retry, post-send uncertainty, missing scope, failed reconciliation without send, malformed-response redaction, closed handles, and injected receipt/proposal rollback. The Workbench composition root now binds the production reply client and Connector-neutral Host orchestration under the real lease with synthetic credentials and Fetch; hosted lifecycle and live credentials remain absent. [Feishu Reply Execution](docs/FEISHU_REPLY_EXECUTION.md) records binding, client, reconciliation, persistence, privacy, audit, and remaining composition limits.
- [x] **TD-208 — Add Connector health, scope, rate-limit, and cursor diagnostics.**
  - Verification record: `@twindesk/plugin-feishu` now exposes `FeishuConnectorDiagnosticsService.health()` through the product-owned ConnectorHealth contract and a richer version 1 `diagnose()` response. Its injected adapter probes configured Bot and User principals separately through exact non-secret credential references, returns operation-specific required and granted scopes, and normalizes per-identity available/limited/unknown rate state; TwinDesk computes missing scopes, treats zero remaining capacity as limited, and never lets Persona configuration imply authorization. Durable cursor snapshots are identity-bound and restricted to explicitly expected streams; diagnostics classify current, stale, future, not-started, or unavailable state while omitting opaque positions and page tokens. One failed identity degrades another usable connection, complete identity failure is unavailable, and scope/rate/cursor problems produce fixed actionable issues. Strict accessor-free parsing, cancellation checks, and payload-free client errors prevent principals, credential locators/values, raw responses, cursor positions, and rejected data from entering output. Synthetic tests cover healthy Bot/User state, missing scopes, authorization loss, zero capacity, rate limiting, stale/future/unavailable cursors, partial/complete probe failure, hostile accessors and arrays, identity mismatch, deterministic redaction, and cancellation. Production diagnostics probes, operation HTTP clients, atomic Keychain rotation, SQLite cursor probes, and settings UI remain unwired; the isolated Keychain reader, credential parser, and refresh transport do not supply them. [Feishu Connector Diagnostics](docs/FEISHU_CONNECTOR_DIAGNOSTICS.md) records semantics, privacy, limits, and TD-209 composition work.
- [ ] **TD-209 — Pass the Feishu closed-loop MVP acceptance tests** from ingestion through local audit.
  - Status: In progress.
  - [x] Local contract acceptance: a synthetic message already admitted by the verified Bot boundary normalizes into one durable `needs_reply` Work Item; healthy Bot/User diagnostics and bounded User context remain read-only; an explicit Persona produces an initial Draft that is superseded by a user-edited revision; the final Draft becomes an exact User-identity proposal, one-time approval, reconcile-before-send success receipt, and six-record local Audit trace. The test restarts after receipt persistence but before Audit append, verifies every referenced durable record, appends the missing deterministic trace without another external effect, rejects terminal execution replay, and verifies credential locators and principals are absent from SQLite. This is acceptance evidence, not an automatic Audit repair service.
  - [x] Production reply request compatibility: newly proposed idempotency keys use a 46-character identity-bound hash within Feishu's 50-character limit; execution revalidates the proposal/configuration binding and rejects over-limit, malformed, or proposal-mismatched keys before client access. Earlier long-format proposals require a new preview and explicit approval rather than an impossible or silently rewritten external request. Synthetic tests cover the platform limit, Bot/User separation, configuration rotation, and fail-closed key validation without weakening exact-key retries.
  - [x] System Keychain read boundary: `FeishuSystemKeychainSecretResolver` maps validated Bot application-credential and User OAuth references to one fixed macOS generic-password service, invokes `/usr/bin/security` without a shell, bounds output to 64 KiB, propagates cancellation, emits payload-free typed errors, and zeroes the resolved byte buffer after callback use on success or failure. Invalid callbacks fail before access, and thrown adapter objects are classified without invoking accessors. Synthetic tests inject the command runner and cover exact arguments, reference/store/purpose/platform rejection, invalid callbacks, missing/empty/oversized values, hostile errors, cancellation, callback failure, and zeroing without reading a live credential. Official API review additionally records that Feishu's one-hour reply `uuid` is absent from reply-history responses, so durable local dispatch evidence remains mandatory before every HTTP send. [Feishu System Keychain Resolution and Replacement](docs/FEISHU_SYSTEM_KEYCHAIN.md) records the mapping, lifetime, privacy, API constraint, and remaining work.
  - [x] Versioned credential-bundle parser: `FeishuCredentialBundleParser` consumes the bounded Keychain bytes, rejects duplicate/unknown fields and invalid UTF-8, binds Bot application credentials or User OAuth credentials to the exact configured app/principal slot, validates canonical lifetimes and sorted `offline_access` scopes, and distinguishes usable access, refresh-required, and reauthorization states with a trusted clock. Source and derived secret buffers are zeroed after callback use, including cancellation and consumer failure; typed errors contain no rejected values. Synthetic tests cover parser failure classes and Keychain-to-parser composition without a live item. The Workbench reply binding now composes User parsing into the exact Host execution path; other operations, hosted lifecycle, and live acceptance remain open. [Feishu Credential Bundles](docs/FEISHU_CREDENTIAL_BUNDLES.md) records the format, official refresh semantics, privacy limits, and remaining work.
  - [x] OAuth v3 refresh contract: `FeishuOAuthV3TokenRefresher` emits one fixed form-encoded `POST https://accounts.feishu.cn/oauth/v3/token` request through a replaceable transport, trusts only the returned actual scopes and lifetime fields, requires a distinct rotated `offline_access` token pair, and classifies invalid/expired/revoked/used refresh tokens as reauthorization rather than blind retry. Requests, responses, and parsed token buffers are bounded and zeroed; cancellation, hostile accessors, malformed responses, service failures, and errors remain payload-free. Synthetic tests make no real request. Live runtime composition remains open. [Feishu OAuth v3 Refresh](docs/FEISHU_OAUTH_V3_REFRESH.md) records the official contract, memory lifetime, recovery rules, and remaining composition.
  - [x] Bounded OAuth v3 HTTP transport: `FeishuOAuthV3HttpTransport` uses Fetch only for the fixed token endpoint with form content, no credentials/referrer/cache, redirect rejection, caller cancellation, a 30-second default timeout, and a two-minute configuration ceiling. It rejects oversized declared responses before reading, enforces the same 32 KiB limit while streaming, clears received chunks, requires JSON for OAuth responses, and preserves retry handling for empty or non-JSON HTTP 429/5xx intermediaries. Synthetic tests inject Fetch and cover exact options, streaming, overflow cancellation and cleanup, redirects, media types, network failure, timeout, cancellation, hostile options, and payload-free errors without a live request. Live runtime composition remains open. [Feishu OAuth v3 Refresh](docs/FEISHU_OAUTH_V3_REFRESH.md) records the transport and remaining rotation boundary.
  - [x] OAuth bundle encoding and Keychain replacement primitive: `FeishuOAuthCredentialBundleEncoder` creates an exact version 1 User bundle from the bound current credential and rotated token set, excludes both prior tokens, requires a distinct refresh token and strictly later acquisition time, enforces scope/size rules, and zeroes the encoded bytes after callback use. `FeishuSystemKeychainSecretReplacer` accepts only the exact User OAuth SecretReference, invokes fixed macOS `security add-generic-password -U` arguments without a shell, keeps the secret out of argv by writing it to stdin, bounds and zeroes diagnostics, and zeroes the supplied bundle on every exit. Every post-start failure is explicitly uncertain. Synthetic parse-back, fresh-instance restart reading, invalid/hostile input, cancellation, command failure, and payload-free tests use injected runners and no live Keychain. The Workbench User reply path now composes the single-Host coordinator and these primitives under the cross-process lease; other hosted credential lifecycles and live acceptance remain open. [Feishu System Keychain Resolution and Replacement](docs/FEISHU_SYSTEM_KEYCHAIN.md) and [Feishu Credential Bundles](docs/FEISHU_CREDENTIAL_BUNDLES.md) record the primitive and remaining composition.
  - [x] Durable single-Host OAuth rotation coordination: `FeishuOAuthRotationCoordinator` reads and identity-binds the current User bundle, fsyncs a secret-free reservation before one remote refresh, composes strict response validation, rotated encoding, and exact Keychain replacement, then fsyncs completion. Its append-only private journal records only sequence, state, and timestamps. Restart reconciles only a strictly newer Keychain `obtainedAt`; the same or older credential becomes uncertain and is never refreshed again, while explicit invalid-token results durably require reauthorization. Synthetic tests cover success, crash recovery, uncertain transport, reauthorization, concurrent calls, cancellation, torn tails, unsafe files, payload absence, and restart without live I/O. The Workbench reply path now runs this coordinator under the cross-process Host lease before reply-dispatch reservation; hosted lifecycle and live acceptance remain open. [Feishu OAuth Rotation Coordinator](docs/FEISHU_OAUTH_ROTATION_COORDINATOR.md) records ordering, recovery, privacy, and limitations.
  - [x] Authorization-code and PKCE exchange boundary: `FeishuOAuthAuthorizationFlow` creates one in-memory 256-bit state plus S256 verifier, emits the exact Feishu authorization URL, accepts only HTTPS or literal-loopback HTTP redirects, and requires an exact single `code`/`state` or `access_denied` callback. A matched callback is consumed before the fixed v3 token transport; replay never exchanges again, while wrong-state and malformed callbacks make no remote call and do not consume the legitimate transaction. Because Feishu codes are five-minute, single-use credentials, every post-start transport or response ambiguity requires a new authorization instead of blind replay. Only actual returned scopes and lifetimes are trusted, `offline_access` is required, and form, response, verifier, client-secret copy, and token buffers are zeroed. Synthetic tests inject randomness and transport and make no live request. The Workbench now composes app-bound registered-loopback capture through verified initial persistence, restart-safe configuration loading, and explicit initial and blocked-state product entries; reconciliation UI and live acceptance remain open. [Feishu OAuth Authorization Code and PKCE](docs/FEISHU_OAUTH_AUTHORIZATION_CODE.md) records the official contract, replay boundary, privacy rules, and remaining work.
  - [x] Post-exchange User principal verification boundary: `FeishuOAuthUserPrincipalVerifier` accepts only an exact configured User identity and bounded access-token bytes, sends a frozen fixed-endpoint request to an injected minimized user-info client, and runs its callback only when the returned application-scoped `open_id` exactly matches the configured principal. The client sees a dedicated token copy that is zeroed on every exit and may return no profile fields beyond `openId`; mismatches, malformed or hostile responses, client failure, and cancellation remain payload-free. A completed persistence callback remains authoritative when cancellation arrives during it, preventing an ambiguous successful write from inviting an unsafe retry. Synthetic tests make no network request. Live composition remains open. [Feishu OAuth User Principal Verification](docs/FEISHU_OAUTH_PRINCIPAL_VERIFICATION.md) records the official identity rule, minimization, privacy, and remaining work.
  - [x] Bounded production User-info HTTP client: `FeishuOAuthUserInfoHttpClient` sends only `GET https://open.feishu.cn/open-apis/authen/v1/user_info` with the exact User Bearer token, JSON acceptance, no ambient credentials/referrer/cache, redirect rejection, caller cancellation, a 30-second default timeout, and a two-minute ceiling. It rejects shared-memory or non-header-safe token bytes, bounds declared and streamed JSON to 16 KiB, clears received chunks, rejects invalid UTF-8 and duplicate object fields, and returns only the application-scoped `openId` while discarding profile fields. Invalid authorization, temporary service/network failures, and terminal User states receive explicit retry dispositions; errors expose no token, profile, principal, response, or thrown data. Synthetic tests inject Fetch and make no live request. Live composition remains open. [Feishu OAuth User Principal Verification](docs/FEISHU_OAUTH_PRINCIPAL_VERIFICATION.md) records the official endpoint, error mapping, minimization, secret lifetime, and remaining composition.
  - [x] Verified initial User credential persistence: `FeishuOAuthInitialCredentialPersister` composes the freshly exchanged token set with the exact configured User, bounded production-compatible `open_id` verifier, initial version 1 bundle encoder, and exact `connector_oauth` Keychain replacement. No bundle is encoded or written until the application-scoped principal matches. Its owned client-secret copy, verifier token copy, exchange token buffers, encoded bundle, and Keychain stdin are cleared at their callback boundaries; shared-memory and hostile inputs fail closed. Identity mismatch and transient post-code verification require reauthorization, while any uncertain Keychain replacement requires reconciliation rather than blind reauthorization. A synthetic end-to-end exchange persists once and is parsed through fresh resolver/parser instances after restart; mismatch, cancellation, invalid input, uncertain write, and payload-free recovery tests use no live account or Keychain item. [Feishu OAuth Verified Initial Persistence](docs/FEISHU_OAUTH_INITIAL_PERSISTENCE.md) records ordering, recovery, privacy, and remaining runtime work.
  - [x] Blocked OAuth state replacement after reauthorization: `FeishuOAuthReauthorizationCoordinator` runs only while the exact rotation journal is `reauthorization_required`, serializes the full same-Host replacement, requires a strictly newer token set, composes the verified initial persister, and then fsyncs an explicit `reauthorized` terminal event. Rotation journal version 2 introduced the distinct terminal state; version 3 now adds a durable replacement reservation before principal verification or Keychain access while reading valid version 1 and 2 history and appending forward without deletion. A second concurrent attempt never verifies or writes; uncertain Keychain writes require `reconcile_keychain`, and any post-write journal ambiguity requires `reconcile_rotation` rather than repeating authorization. Synthetic tests cover exact verified replacement and restart, v1/v2-to-v3 migration, mismatch, stale chronology, concurrency, non-pending and hostile input, uncertain persistence, ambiguous journal completion, payload absence, and recovery without a live account or Keychain item. [Feishu OAuth Reauthorization Replacement](docs/FEISHU_OAUTH_REAUTHORIZATION.md) records ordering, migration, recovery, privacy, and remaining runtime composition.
  - [x] Operation-level scope authorization: `FeishuOperationScopeAuthorizer` owns fixed version 1 policies for Bot reply, User reply, and User message discovery, binds each operation to the exact configured identity, validates the trusted clock before probing, and invokes an operation callback only after an exact fresh authorization observation contains every required scope. Persona and callers cannot add, remove, alias, or substitute scopes or identities. Unknown operations, missing identities, stale/future or mismatched evidence, missing authorization/scope, cancellation, and hostile adapters fail before the operation; a successfully completed callback remains authoritative if cancellation arrives during it. Synthetic tests use no live account or credential. Concrete Bot and User probes now compose through the Workbench Host reply binding; context endpoint policies, other operation HTTP clients, hosted lifecycle, and live acceptance remain open. [Feishu Operation Scope Authorization](docs/FEISHU_OPERATION_SCOPE_AUTHORIZATION.md) records policy, freshness, authority, privacy, and remaining work.
  - [x] User Keychain credential scope probe: `FeishuUserCredentialScopeProbe` accepts only the exact configured User reply or discovery request and fixed required-scope list, samples one trusted time, resolves the exact `connector_oauth` Keychain item, identity-binds and parses it, and returns only the usable token's issued scopes plus local observation time. Access-token expiry requires durable rotation before retry; expired refresh authorization or a missing item requires reauthorization. Bot or substituted operations, identities, references, scopes, malformed local credentials, invalid clocks, cancellation, and hostile values fail before operation access; Keychain and derived secret buffers are zeroed. This is a fresh read of stored token claims, not remote permission introspection, so HTTP authorization failures remain authoritative. Synthetic tests touch no live item. The Workbench Host reply binding now rotates first, composes this probe, and rechecks the actual send bundle; other operation HTTP clients and live acceptance remain open. [Feishu User Credential Scope Probe](docs/FEISHU_USER_CREDENTIAL_SCOPE_PROBE.md) records ordering, evidence meaning, refresh, privacy, and remaining work.
  - [x] Bounded Bot tenant-token acquisition: `FeishuBotTenantTokenAcquirer` sends one fixed JSON `POST` to Feishu's internal tenant-token endpoint with no redirects, ambient credentials, referrer, or caching and a 30-second timeout bounded by a two-minute configuration ceiling. It validates the exact success shape and trusted-clock lifetime, rejects oversized declared or streamed JSON above 16 KiB, clears request, response, stream-chunk, and callback token buffers, preserves caller ownership of the borrowed application-secret buffer, and emits payload-free retry or configuration-repair failures while treating Feishu's application-level rate-limit code as retryable and unknown codes as fail-closed. A completed consumer result remains authoritative if cancellation arrives during it. Acquiring the token proves neither Bot scope nor approval. Synthetic tests inject Fetch and cover exact escaped JSON, cleanup, response bounds, malformed and duplicate data, invalid credentials, rate/service/network/timeout failures, cancellation, hostile values, and consumer failure without live I/O. Keychain, Bot scope, and reply HTTP composition are covered by the Workbench Host binding; Bot full-stack testing and live acceptance remain open. [Feishu Bot Tenant Token Acquisition](docs/FEISHU_BOT_TENANT_TOKEN_ACQUISITION.md) records the contract and limits.
  - [x] Bot Keychain credential and current-scope probe: `FeishuBotKeychainScopeProbe` accepts only the exact configured Bot reply policy, resolves and parses the exact `connector_app_credential` Keychain item, acquires one callback-scoped tenant token, then uses a fixed-endpoint bounded client to verify the current remote Bot `open_id` and retain only application scopes whose `token_types` includes `tenant`. User-only scopes cannot authorize the Bot. Missing credentials, rejected application authorization, principal mismatch, missing scopes, malformed or oversized responses, rate and service failures, substituted evidence, invalid clocks, cancellation, and hostile values fail before the operation callback with fixed recovery classes and payload-free errors. Keychain, application-secret, request, response, stream-chunk, and tenant-token buffers are cleared after use. Synthetic tests make no live Keychain or network access. The Workbench Host factory accepts this probe and reacquires a callback-scoped send token through the adapter; Bot full-stack testing, diagnostics wiring, and live acceptance remain open. [Feishu Bot Keychain Scope Probe](docs/FEISHU_BOT_KEYCHAIN_SCOPE_PROBE.md) records the evidence semantics, privacy limits, and remaining work.
  - [x] Exclusive cross-process Feishu runtime lease: `FeishuRuntimeLeaseManager` holds one deterministic kernel-owned loopback endpoint for the complete Host callback, conservatively excluding every competing Feishu runtime even across different configured accounts. The endpoint derives only from a permanent cross-release namespace and carries no protocol data; account, application, principal, and credential identifiers never enter the address or errors. A future implementation must keep acquiring this endpoint while released builds may exist. Contention fails closed, cancellation before acquisition invokes no callback, callback failure releases ownership without replacing the original failure, and `assertHeld()` provides a mandatory pre-operation guard without granting authority. Real loopback tests prove same-process and child-process exclusion, clean restart, and automatic release after `SIGKILL` without stale PID, heartbeat, or filesystem recovery. The Workbench reply path now holds the lease across OAuth rotation, durable dispatch, reply HTTP, and receipt/Audit persistence; hosted lifecycle and other Feishu operations remain open. [Feishu Runtime Lease](docs/FEISHU_RUNTIME_LEASE.md) records lifecycle, authority, privacy, collision handling, and remaining work.
  - [x] Durable pre-send dispatch journal: SQLite migration 6 adds ordered, identity-bound `action_dispatches` reservations. `FeishuReplyExecutor.execute()` now performs exact reconciliation first when supported and always requires a durable reservation immediately before its injected client may send; a missing coordinator, reservation failure, or existing unsettled/uncertain dispatch sends nothing. A send-only client can use only the first reservation unless a prior attempt durably settled as `retry_same_key`. Receipt persistence settles the latest reservation in the same transaction as proposal state. Restart tests cover a crash after reservation but before receipt, fail-closed resend prevention, known retry, receipt rollback, closed handles, schema migration, and Thread export/deletion. The journal stores no content, credential reference/value, principal, or raw remote payload. [Durable Action Dispatch Journal](docs/ACTION_DISPATCH_JOURNAL.md) records the state machine, conservative recovery behavior, retention, and remaining production composition.
  - [x] Bounded reply HTTP primitive: `FeishuReplyHttpClient` sends one plain-text `POST` only to the fixed Feishu message-reply endpoint with the exact target, nested content JSON, Bearer token, and 46-character proposal key as the platform `uuid`. It rejects hostile or shared-memory inputs before Fetch, bounds declared and streamed JSON to 256 KiB, rejects redirects and duplicate or invalid UTF-8 JSON, clears request, response, and stream buffers, and returns only the generated message ID and source timestamp. Known authorization, missing-scope, rate-limit, and explicit invalid-request responses remain distinct; every network, timeout, service, unknown-code, or malformed-success result is conservatively ambiguous after `POST` and cannot authorize a blind resend. It intentionally does not implement remote reconciliation because Feishu reply history does not expose `uuid`. Synthetic tests inject Fetch and use no live credential or request. The Workbench composition root now binds the lease-, Keychain-, scope-held client and Connector-neutral Host orchestration; hosted and live execution remain open. [Feishu Reply HTTP Client](docs/FEISHU_REPLY_HTTP_CLIENT.md) records the protocol, privacy, uncertainty, and reconciliation limits.
  - [x] Reconciliation-optional dispatch safety: `FeishuReplyExecutionClient.reconcile` is now optional instead of forcing the production adapter to invent remote absence. Exact clients retain reconcile-before-send and can settle an uncertain dispatch later. A send-only client may call `send()` only after the durable coordinator creates the first reservation; an existing, unsettled, uncertain, successful, or terminally failed reservation blocks it, while only a durably settled `retry_same_key` rejection permits another ordinal. Explicit reconciliation without capability returns a payload-free non-retryable uncertain receipt, and post-send network/unknown failures are not advertised as retryable when no exact lookup exists. Synthetic restart tests prove first-send success, blocked resend, unsupported reconciliation, safe rate-limit retry, hostile capability redaction, durable settlement, and zero calls before reservation. The Workbench composition root now binds the production send-only adapter and Connector-neutral Host orchestration; hosted and live execution remain open. [Feishu Reply Execution](docs/FEISHU_REPLY_EXECUTION.md) and [Durable Action Dispatch Journal](docs/ACTION_DISPATCH_JOURNAL.md) record both paths and their recovery limits.
  - [x] Lease-held Bot/User reply execution adapter: `FeishuReplyExecutionAdapter` is a send-only production composition that accepts only the exact configured execution request and an already-held Host lease, routes the User path through optional durable OAuth rotation and then the concrete User scope probe, routes Bot through its remote scope probe, re-resolves the exact Keychain credential for dispatch, and calls the bounded reply HTTP client only after a final lease check. The actual User send token must remain usable and retain `im:message:send_as_user`; the Bot path reacquires a callback-scoped tenant token after remote principal and tenant-scope verification. Temporary pre-POST failures settle as `preflight_unavailable` with `retry_same_key`; reauthorization-required and uncertain rotation produce distinct terminal receipts; HTTP network ambiguity remains non-retryable for this send-only adapter. Synthetic tests cover exact Bot/User ordering, identity and scope mismatch, successful rotation, rotation recovery classes, cancellation and lease failure, token/body cleanup, payload-free errors, incomplete composition, and executor dispatch settlement without live I/O. The Workbench composition root binds the Connector-neutral Host orchestration and requires User rotation for configured User identity; hosted lifecycle and live acceptance remain open. [Feishu Reply Execution Adapter](docs/FEISHU_REPLY_EXECUTION_ADAPTER.md) records ordering, retry boundaries, secret lifetime, and limitations.
  - [x] Connector-neutral Host action execution: `WorkHubActionExecutionHost` accepts only exact Approval and proposal IDs, runs the complete operation inside an injected exclusive-ownership callback, recomputes durable approval bindings, consumes one opaque `ApprovedAction`, durably appends approval Audit before execution may start, owns the dispatch-reservation callback, persists receipt/proposal/dispatch settlement, and then appends a deterministic per-receipt execution Audit record. An approval Audit failure blocks dispatch; restart repairs it without reviving authority if the approval expired. A retryable receipt's Audit is repaired before another ordinal may execute, while a terminal receipt repairs missing execution Audit after restart and expiration without invoking the Connector again. Audit IDs bind canonical receipt digests while details exclude content, targets, external IDs, idempotency keys, credential references, payloads, and thrown values. Synthetic tests prove ownership/cancellation fail before consumption, approval-Audit fail-closed recovery, one external effect, retry ordinal two, receipt/Audit interruption recovery, post-expiry repair, and payload-free errors. [Work Hub Action Execution Host](docs/ACTION_EXECUTION_HOST.md) records ordering, recovery, privacy, and remaining work.
  - [x] Workbench Feishu reply runtime binding: `createWorkbenchFeishuReplyExecutionHost()` composes the Connector-neutral Host operation, default kernel-backed `FeishuRuntimeLeaseManager`, `FeishuReplyExecutor`, lease-held `FeishuReplyExecutionAdapter`, exact Bot/User scope collaborators, Keychain resolver, User rotation coordinator, Bot token acquirer, and bounded reply HTTP client. It validates concrete collaborators before execution and derives every authority-bearing field from durable records. A synthetic full-stack User test proves approval and Audit ordering, one expired token rotation, three callback-scoped credential reads, one durable reply dispatch and HTTP request, secret/body cleanup, receipt settlement, and fresh-instance recovery after approval expiry with no resend. It uses the real loopback lease but no live Keychain item, credential, account, or network endpoint. Cordis lifecycle activation remains open. [Workbench Feishu Reply Runtime](docs/WORKBENCH_FEISHU_REPLY_RUNTIME.md) records the boundary and limitations.
  - [x] Lease-held User OAuth rotation preflight: the Workbench factory requires a concrete `FeishuOAuthRotationCoordinator` whenever User identity is configured, and the reply executor invokes the adapter's explicit preparation phase only after Host approval consumption, Audit, and execution start but before durable reply dispatch reservation while the same kernel lease remains held. A needed refresh durably reserves its rotation journal and replaces the exact Keychain item; only then may the executor reserve reply dispatch and run scope and send-time credential reads before reply HTTP. Temporary pending or journal failures remain `retry_same_key` without a reply reservation; reauthorization-required and uncertain rotation become distinct terminal reply receipts so neither OAuth nor reply dispatch is blindly repeated. Synthetic tests prove one rotation and one reply, absence of reply dispatch during rotation, zero scope/reply calls for rotation recovery errors, payload-free mapping, transient buffer cleanup, durable receipt settlement, and no resend after restart.
  - [x] Lease-held blocked-state reauthorization replacement: `createWorkbenchFeishuOAuthReauthorizationHost()` requires an exact configured User, a concrete `FeishuOAuthReauthorizationCoordinator`, and the same kernel-backed Host lease used by the reply runtime. It accepts only already-exchanged replacement evidence, asserts ownership immediately before coordinator replacement, and preserves the coordinator's verified principal, newer-chronology, Keychain, journal, and explicit recovery boundaries. It neither starts authorization nor retries an approved reply. A synthetic full composition uses the real coordinator, verifier, replacement primitive, journal, and an observable lease manager to prove ownership remains held during verification and Keychain replacement, internal secret cleanup, `reconcile_keychain` after post-start cancellation, clean release, pre-acquisition cancellation, User-only composition, and hostile-accessor rejection; the separate runtime-lease suite retains real cross-process and crash-release evidence for the production manager. Caller-owned source buffers retain their original ownership and must remain callback-scoped. [Workbench Feishu OAuth Reauthorization Runtime](docs/WORKBENCH_FEISHU_OAUTH_REAUTHORIZATION_RUNTIME.md) records authority, recovery, privacy, and remaining hosted work.
  - [x] Hosted blocked-state reauthorization runtime: `createWorkbenchFeishuOAuthHostedReauthorizationHost()` constructs its coordinator from the exact injected persister and recovery journal, then holds one Feishu lease from a fail-closed `reauthorization_required` preflight through registered-loopback listener binding, S256 PKCE presentation, state-bound callback capture, one code exchange, application-scoped principal verification, exact Keychain replacement, and fsynced `reauthorized` settlement. It rechecks the journal before consuming the code; non-pending state opens no authorization or transport, while reserved/uncertain state routes to rotation reconciliation. `loadWorkbenchFeishuOAuthHostedReauthorizationHost()` reconstructs the exact callback from fresh identity and authorization Settings, and the default loader binds the bounded production token transport, minimized User-info verifier, and system-Keychain replacer around the supplied concrete journal without performing an effect during construction. The composition root remains responsible for supplying the default-path journal. Synthetic real-loopback, restart, lease-observation, transient-zeroing, non-pending, payload-absence, and hostile-input tests make no live request or Keychain access. The product entry, local-only evidence reconciliation action, and its business Audit are now composed separately; credential repair, Cordis activation, and live acceptance remain open. [Workbench Feishu OAuth Reauthorization Runtime](docs/WORKBENCH_FEISHU_OAUTH_REAUTHORIZATION_RUNTIME.md) records the ordering and limits.
  - [x] Hosted Feishu User OAuth reauthorization product entry: the default Workbench Web composition now supplies the concrete recovery journal to a separate memory-only controller around the hosted blocked-state runtime. `GET /api/reauthorization/feishu` exposes only exact idle/starting/waiting/succeeded/cancelled or minimized recovery state and a distinct process-local CSRF capability. Start accepts a 1–512-byte transient app secret only when a fresh durable recovery read is exactly `reauthorization_required`; Host/Origin/Fetch-Metadata/media/body checks, independent Web/controller validation, strict Feishu URL parsing, explicit browser click, polling, cancellation, competing-attempt rejection, and buffer zeroing fail closed. `reconciliation_required`, `reconcile_keychain`, and `reconcile_rotation` never become automatic retry. Synthetic controller, local API, hostile-value, recovery-gate, restart-composition, and shutdown tests use no live account, Feishu request, or Keychain item. Reconciliation action and Audit are now composed separately; credential health/disconnect/revocation, Cordis activation, polling, live reauthorization, and Stage 2 acceptance remain open. [Workbench Feishu OAuth Reauthorization UI](docs/WORKBENCH_FEISHU_OAUTH_REAUTHORIZATION_UI.md) records the boundary and limits.
  - [x] Durable reauthorization replacement reservation: rotation journal version 3 adds `reauthorization_reserved` and fsyncs it after exact blocked-state validation but before principal verification or the Keychain replacement boundary. A successful attempt appends `reauthorized`; a known pre-write validation, verification, or cancellation failure safely restores `reauthorization_required`; an uncertain Keychain outcome or interrupted process leaves the durable reservation and projects only `reconciliation_required` after restart. A fresh rotation coordinator may settle it as `reauthorized` only after the exact configured Keychain bundle proves a strictly newer credential, without repeating OAuth, refresh, or Keychain write. Version 1 and 2 history remains readable; new transitions append forward without deletion. Synthetic success, uncertain-write, known-safe rollback, newer-Keychain restart recovery, default Web restart presentation, migration, hostile-callback, and secret-absence tests use no live account or Keychain. The explicit product reconciliation action and its business Audit are now composed separately. [Feishu OAuth Reauthorization Replacement](docs/FEISHU_OAUTH_REAUTHORIZATION.md) records the durable ordering and recovery boundary.
  - [x] Explicit local OAuth reconciliation action: `FeishuOAuthReconciler` owns only an exact Keychain resolver, identity-bound evidence parser, and rotation journal; it has no refresh transport or Keychain replacer. Under the exclusive Workbench Feishu lease it accepts only restart-visible `reserved`, `uncertain`, or `reauthorization_reserved` evidence and appends `completed` or `reauthorized` only when the exact configured bundle is strictly newer and its refresh authorization remains usable. Same/older or expired evidence remains blocked, so historical write proof cannot be presented as current recovery. The default Web composition exposes a distinct process-local CSRF capability and `POST /api/recovery/feishu/oauth/reconcile`; independent recovery gating, Host/Origin/Fetch-Metadata/media/body validation, minimized results, result-state recheck, explicit Connectors-page click, and shutdown cancellation fail closed. Synthetic rotation, reauthorization, restart, expiry, identity mismatch, secret cleanup, lease, hostile-input, request-forgery, replay, and browser-contract tests use no live account, network request, or real Keychain item. Durable business Audit is now composed separately; credential repair/removal, revocation, hosted polling, and live acceptance remain open. [Workbench Feishu OAuth Reconciliation](docs/WORKBENCH_FEISHU_OAUTH_RECONCILIATION.md) records authority, privacy, recovery, and limits.
  - [x] Connector-scoped business Audit reference boundary: version 1 Audit records may use an intrinsic stable `connector` reference without inventing a local Connector entity row. SQLite Migration 7 advances the database compatibility boundary, validates pre-existing kinds, and rejects unsupported inserts so older builds fail closed before parsing newer data. The reference remains exact-queryable and restart-safe, flows through the minimized Web contract, and grants no credential, scope, or write authority. A Connector-only maintenance record has no implicit Thread ownership and is excluded from unrelated Thread export/deletion while remaining globally retained. Synthetic domain, migration, SQLite, restart, browser-contract, export, deletion, and redaction tests use no real account or Connector payload. Reconciliation request/result orchestration now uses the separate restart-repairable maintenance protocol.
  - [x] Connector maintenance request/result Audit protocol: SQLite Migration 8 adds one Connector-neutral pending operation whose fixed request Audit commits atomically before an effect and whose immutable settlement commits atomically with its fixed result Audit. Exactly one credential reconciliation may remain pending per Connector; exact request/result replay is idempotent, conflicting evidence fails closed, and interruption after the Connector effect remains restart-discoverable rather than losing Audit or repeating authority. Connector-only operation and Audit history is excluded from unrelated Thread export/deletion and stores no account, application, principal, SecretReference, credential, scope, journal sequence, path, payload, or hidden reasoning. Synthetic migration, atomicity, restart, interruption, conflict, hostile-input, retention, and redaction tests perform no Keychain or network action. [Connector Maintenance Audit Protocol](docs/CONNECTOR_MAINTENANCE_AUDIT.md) records ordering, authority, recovery, privacy, and retention.
  - [x] Workbench Feishu reconciliation Audit wiring: the reconciliation service now commits the Connector maintenance request Audit while holding the Feishu Host lease and before any Keychain read, then atomically settles the operation with `reconciled`, `still_required`, `cancelled`, or `failed` result Audit. The default Web composition owns one shared business database handle, repairs a pending operation before accepting requests, exposes its Audit through the same projection even in memory, and closes it after HTTP shutdown. Restart repair reads only the pending SQLite row and secret-free rotation journal: terminal journal evidence recorded at or after the request becomes `reconciled`, unresolved crash-visible evidence becomes `still_required`, older evidence cannot be misattributed, and no repair path resolves Keychain or repeats the reconciliation effect. Durable terminal success remains authoritative over cancellation arriving during post-effect cleanup. Synthetic direct and default-composition tests cover request-before-effect ordering, success, failure, cancellation, restart repair, historical terminal evidence, zero Keychain repair reads, Audit presentation, and shared database lifecycle without live credentials or network I/O. [Workbench Feishu OAuth Reconciliation](docs/WORKBENCH_FEISHU_OAUTH_RECONCILIATION.md) and [Connector Maintenance Audit Protocol](docs/CONNECTOR_MAINTENANCE_AUDIT.md) record the boundary.
  - [x] Literal-loopback OAuth callback Host: `FeishuOAuthLoopbackCallbackHost` binds only `127.0.0.1` or `::1`, returns its exact dynamically allocated redirect URI, arms once against the existing flow's fixed Feishu authorization URL and state, and captures one exact success or denial callback without performing exchange or persistence. Wrong-state, duplicate, unknown, wrong-path, body-bearing, non-GET, absolute, and oversized requests receive fixed payload-free 404 responses without consuming the legitimate callback. Success, denial, five-minute-bounded timeout, cancellation, close, and listener failure release the server; responses disclose no request values and disable caching, referrers, framing, and content sniffing. Real ephemeral-loopback tests cover lifecycle, hostile inputs, and malformed-request survival without external I/O. The Workbench product entry now consumes this primitive and presents an explicit browser link; recovery UI and live acceptance remain open. [Feishu OAuth Loopback Callback Host](docs/FEISHU_OAUTH_LOOPBACK_CALLBACK_HOST.md) records ordering, security, and limits.
  - [x] Workbench lease-held initial OAuth authorization: `createWorkbenchFeishuOAuthAuthorizationHost()` composes one configured literal-loopback listener, one state-bound PKCE session, callback capture, exchange, exact User principal verification, and initial Keychain persistence while the same exclusive Feishu Host lease remains held. It checks that the exact User Keychain item is absent before presentation and again before consuming the code, so an existing credential routes to reauthorization instead of being silently replaced, and the persister rechecks ownership immediately before Keychain replacement so lease loss during verification writes nothing. Its frozen presenter view contains only the authorization and redirect URLs and cannot extend the listener timeout; failure, denial, cancellation, or a stalled presenter closes the port and releases ownership. Real-loopback synthetic tests prove the complete ordering, lease checks, two absence probes, final write guard, one replacement, and transient cleanup without a live account, endpoint, browser, or Keychain item. The product now supplies separate initial and blocked-state browser presentation and cancellation; reconciliation UI, Cordis lifecycle activation, and live acceptance remain open. [Workbench Feishu OAuth Authorization Runtime](docs/WORKBENCH_FEISHU_OAUTH_AUTHORIZATION_RUNTIME.md) records the boundary and limitations.
  - [x] Registered OAuth authorization configuration: version 1 `FeishuOAuthAuthorizationConfiguration` binds one Feishu app to unique sorted `offline_access` scopes and one canonical literal-loopback HTTP redirect with an explicit fixed non-default port. It rejects port zero, `localhost`, non-loopback or HTTPS hosts, query/fragment/userinfo, encoded path aliases, unknown fields, hostile accessors, and app mismatches. The Workbench Host requires the actual bound listener URI to equal this configured URI before presentation or Keychain access, so a mismatch closes the listener and performs no secret read, exchange, or persistence. Synthetic tests cover IPv4/IPv6, immutability, malformed configuration, exact app binding, and fail-closed listener mismatch without a live Feishu registration. Existing-identity replacement, reconciliation UI, and live registration acceptance remain open. [Feishu OAuth Authorization Configuration](docs/FEISHU_OAUTH_AUTHORIZATION_CONFIGURATION.md) records the boundary and limitations.
  - [x] Restart-safe OAuth authorization settings store: `FeishuOAuthAuthorizationConfigurationStore` validates the exact version 1 non-secret app/redirect/scope shape before disk access, writes through a private `0600` temporary file with file flush, atomic rename, and parent-directory flush, and reads with `O_NOFOLLOW`, strict UTF-8 decoding, and a race-safe 64 KiB bound. Missing files are explicit; symlinks, directories, oversized or corrupt documents, unsupported versions, invalid paths, and undeclared secret-like fields fail closed without changing the prior valid document. Restart tests prove exact recovery, validation-rejected-write rollback, file mode, and absence of client secrets, codes, verifiers, tokens, and private keys. Existing-identity replacement, reconciliation UI, and live registration acceptance remain open. [Feishu OAuth Authorization Configuration](docs/FEISHU_OAUTH_AUTHORIZATION_CONFIGURATION.md) records persistence and limits.
  - [x] Restart-loaded OAuth authorization runtime: `loadWorkbenchFeishuOAuthAuthorizationHost()` reads fresh identity and authorization store instances, requires both versioned documents, an exact User identity, and one matching app before it creates a callback Host. It derives only an explicitly whitelisted IPv4 or IPv6 literal host, fixed port, and path from the validated registered redirect and delegates to the existing lease-held authorization composition. Missing, Bot-only, mismatched, or hostile Settings return fixed payload-free failures before loopback binding, Keychain access, browser presentation, or network exchange. The resulting Host owns an immutable load-time snapshot; later Settings edits require reconstruction. A synthetic IPv6 restart test persists both documents, reconstructs the runtime, binds the exact registered port, captures the callback, verifies the principal, and writes once; failure tests prove zero lease or secret access while existing runtime tests retain IPv4 coverage. The product now supplies initial and blocked-state authorization presentation; reconciliation UI, Cordis lifecycle activation, and live acceptance remain open. [Workbench Feishu OAuth Authorization Runtime](docs/WORKBENCH_FEISHU_OAUTH_AUTHORIZATION_RUNTIME.md) records the boundary and limits.
  - [x] macOS Workbench local Settings paths: `resolveWorkbenchLocalDataPaths()` selects `~/Library/Application Support/TwinDesk` independently of the checkout and reserves versioned Feishu identity and OAuth authorization documents below `settings/connectors/feishu`. `openWorkbenchFeishuSettingsStores()` walks fixed path components, rejects observed symlinks and non-directories, requires effective-user ownership plus exact `0700` modes for product directories, and rechecks directory device/inode identities before returning the concrete restart-safe Connector stores. Relative, root, aliased, unsupported-platform, unknown-field, and hostile-accessor inputs fail with fixed payload-free errors. Synthetic temporary-home tests prove exact paths, owner-private modes, restart recovery, no external symlink writes, and absence of secret-like fields without touching the real home; this preparation check is not a same-user filesystem lock after return. Identity/SecretReference editing, disconnect/deletion/revocation, other production data paths, Cordis lifecycle activation, and live acceptance remain open. [Workbench Local Data Paths](docs/WORKBENCH_LOCAL_DATA_PATHS.md) and [ADR 0003](docs/decisions/0003-macos-local-data-root.md) record the boundary and decision.
  - [x] Presentation-safe Feishu Settings status: `createWorkbenchFeishuSettingsPresentation()` reads only the concrete restart-safe identity and OAuth Settings stores and emits one frozen, versioned snapshot containing identity types, literal-loopback host/port, requested scopes, and an app-match boolean. It omits application, account, principal, display-name, SecretReference, redirect-path, and filesystem identifiers. The loopback Web server accepts the reader only through its composition options, revalidates the exact minimized contract before serialization, serves only `GET`/`HEAD`, and returns a fixed `503` for missing, failed, or invalid readers; the Connectors page labels internally complete configuration as **Settings ready** without implying credential validity, current authorization, connectivity, or write authority. `startWorkbenchWebServer()` opens the fixed macOS stores and supplies that reader, and the default `web:start` launcher uses this composition without introducing a Web-to-Connector dependency. Synthetic empty/partial/mismatch/restart, hosted default-path, hostile/corrupt input, browser rejection, server failure, and leakage tests use no real home, Keychain, account, or external network. Cordis activation, existing-identity/credential replacement, health/recovery UI, disconnect/revocation, and live acceptance remain open. [Workbench Feishu Settings Presentation](docs/WORKBENCH_FEISHU_SETTINGS_PRESENTATION.md) records the boundary and limits.
  - [x] CSRF-protected Feishu OAuth Settings editing: `createWorkbenchFeishuOAuthSettingsEditor()` is a writer separate from the read-only presenter. It requires an existing User identity, derives its app ID locally, fixes the callback path, and persists only a canonical literal-loopback host/port plus unique sorted `offline_access` scopes through the existing atomic private store. The browser never reads or submits the app, account, principal, display name, SecretReference, credential, redirect path, or filesystem location. The default Web composition exposes the writer only through `POST /api/settings/feishu` with exact bound Host and Origin, `Sec-Fetch-Site: same-origin`, a process-local 256-bit CSRF header token obtained from the same-origin status response, exact JSON media type, declared non-streaming body at most 16 KiB, and independent Web, Workbench, and Connector validation. Cross-site, missing-token, media, query, malformed, accessor, sparse, oversized, missing-User, and writer failures fail before mutation with fixed payload-free responses. Synthetic same-value replay, IPv4/IPv6, restart, hostile input, default launcher persistence, and loopback request tests use no real home, Keychain, account, credential, browser authorization, or external network. Existing-identity replacement, reconciliation UI, Settings-change Audit, deletion/revocation, Cordis activation, and live acceptance remain open. [Workbench Feishu OAuth Settings Editing](docs/WORKBENCH_FEISHU_OAUTH_SETTINGS_EDITING.md) records the boundary and limitations.
  - [x] Create-only Feishu User identity bootstrap: `createWorkbenchFeishuUserIdentityBootstrapper()` lets an empty local installation create one User identity or a Bot-only connection add its User slot. The browser submits only a new-connection app ID, display name, and User `open_id`; Workbench generates the opaque account ID and `system_keychain` OAuth SecretReference, while Bot-only creation preserves the existing account, app, Bot, and Bot reference without disclosing them. It never accepts or creates a credential, reads Keychain, starts OAuth, calls Feishu, or implies authorization. A fresh store read inside the serialized Settings mutation makes the operation create-only and permits at most one competing request. The minimized status body remains identifier-free and advertises only `new` or `existing` capability in a response header; `POST /api/settings/feishu/user-identity` reuses the exact Host/Origin/Fetch-Metadata/CSRF/media/body boundary and independent schema validation. Synthetic empty/Bot-only, opaque-ID, restart, concurrency, hostile-field, request-forgery, and default Web composition tests use no real account, credential, Keychain item, or external network. Existing User replacement/editing, Bot creation/editing, reconciliation UI, Settings-change Audit, deletion/revocation, Cordis activation, and live acceptance remain open. [Workbench Feishu User Identity Bootstrap](docs/WORKBENCH_FEISHU_USER_IDENTITY_BOOTSTRAP.md) records the boundary and limitations.
  - [x] Initial Feishu User OAuth product entry: the Connectors page starts one explicit initial authorization only after minimized Settings are ready, sends a bounded app-secret body through the same-origin loopback boundary, clears TwinDesk-owned browser/server/controller buffers, and waits for the existing lease-held Host to expose one strictly validated Feishu URL. The browser opens that URL only after a user click, polls memory-only status, supports cancellation, and distinguishes verified Keychain persistence from connectivity or scope health. Workbench and Web independently minimize and validate states; competing starts, hostile URLs, request forgery, malformed bodies, invalid Host results, and shutdown all fail closed. Existing credentials route to reauthorization and uncertain writes to reconciliation without automatic retry. Synthetic tests use no live Feishu account, network endpoint, or Keychain item. Reauthorization/reconciliation UI, credential health/disconnect/revocation, Settings-change Audit, Cordis activation, polling, live authorization, and Stage 2 acceptance remain open. [Workbench Feishu OAuth Authorization UI](docs/WORKBENCH_FEISHU_OAUTH_AUTHORIZATION_UI.md) records the boundary and limitations.
  - [x] Default secret-free OAuth recovery journal path: version 2 of the macOS Workbench path contract reserves `state/connectors/feishu/oauth-rotation.jsonl` below the fixed private product root and returns the concrete journal with the two Connector Settings stores. Settings and operational state remain separate; every product-owned state directory is owner-bound, exact `0700`, symlink-rejected, and rechecked before use, while the journal retains `0600`, `O_NOFOLLOW`, bounded append, fsync, torn-tail repair, and versioned transition validation. Restart tests recover the exact terminal event and scan all three files for secret-like fields; a linked state branch fails before journal access. Hosted reauthorization, local-only evidence reconciliation, and restart repair of its business Audit now consume this path; credential repair, Cordis activation, and live acceptance remain open. [ADR 0004](docs/decisions/0004-feishu-oauth-recovery-journal-path.md) and [Workbench Local Data Paths](docs/WORKBENCH_LOCAL_DATA_PATHS.md) record the decision and limits.
  - [x] Presentation-safe OAuth recovery status: `createWorkbenchFeishuOAuthRecoveryPresentation()` maps the default rotation journal to only `not_started`, `ready`, `rotation_active`, `reauthorization_required`, or `reconciliation_required`, omitting journal sequence, timestamps, identities, SecretReferences, credentials, scopes, and filesystem paths. An absent journal means no rotation history rather than no credential; settled evidence means no unresolved journal transaction rather than credential health. A reserved event is active only when the same process proves ownership and otherwise requires reconciliation. The default Workbench Web composition injects this reader through `GET`/`HEAD /api/recovery/feishu/oauth`; Web independently validates the exact three-field response, rejects queries and mutations, and returns a fixed `503` for unavailable or invalid state. Both the Connectors page and authorization-start endpoint block another initial authorization while recovery state is unavailable, active, or unresolved; the server check runs before the app-secret body is accepted or the Host invoked. Exact `reauthorization_required` evidence permits only the separate replacement action; `reconciliation_required` permits only the separate local evidence comparison and never an automatic OAuth retry or Keychain write. Synthetic mapping, restart, hostile-accessor, leakage, API, direct-POST-gate, and browser-source tests use no live Keychain or account. Reconciliation Audit is now composed separately; credential health/repair/disconnect/revocation, Cordis activation, and live acceptance remain open. [Workbench Feishu OAuth Recovery Presentation](docs/WORKBENCH_FEISHU_OAUTH_RECOVERY_PRESENTATION.md) records the semantics and limits.
  - [x] Durable model-backed Draft linkage boundary: `createWorkHubModelDraftLinkage()` accepts one caller-supplied Harness Session/Run identity only after the caller has flushed that Session, plus one bounded version 1 `editing` Draft; it stores the Draft idempotently and appends a deterministic Persona-attributed run Audit with the complete opaque WorkItem → Session → Run chain. It copies no prompt, context, model output, Tool data, credential, vendor binding, or hidden reasoning into Audit; output remains only in the bounded local Draft, and an optional rationale must be a bounded user-visible summary. If Audit persistence is interrupted after Draft creation, exact replay repairs only Audit without repeating or authorizing a model Run. Synthetic restart, interruption, replay, reference, bounds, closed/conflicting/malformed storage, hostile-input, privacy, and payload-free error tests use no model or external I/O. [Model-Backed Draft Linkage](docs/MODEL_BACKED_DRAFT_LINKAGE.md) records ordering, authority, recovery, and privacy.
  - [x] Pinned Harness model-Draft runtime composition: `createHarnessModelDraftRunner()` creates one real pinned Harness Agent under the Persona-mapped Preset, requires a participating `SessionStore.flush()`, disposes the live Agent, and validates the completed turn again from cold persistence before returning bounded visible text. Exact retry with the same Session identity validates the stored prompt, Preset, provider/model provenance, and single completed turn, then returns `recovered` with zero model calls; incomplete, cancelled, failed, max-token, conflicting, non-text, or malformed evidence fails closed. `createWorkHubHarnessModelDraftOperation()` preflights the Work Item and selected Persona before invocation, converts only visible text into an `editing` Draft, and enters the restart-repairable Draft/Audit linkage. An existing Draft forces `recover_only`: a missing Harness Session cannot authorize another model call, while exact repair remains available after Draft transition or a later Persona selection change. Keyless tests use the real Agent Loop and JSONL Session persistence but no live provider or external I/O. Product UI/Workbench entry, provider selection/configuration, interactive editing/approval, polling, and live acceptance remain open. [Harness Model Draft Runtime](docs/HARNESS_MODEL_DRAFT_RUNTIME.md) records the boundary.
  - [ ] Live-account exit acceptance: production Feishu authorization-code/PKCE exchange through verified initial Keychain persistence, durable User rotation, blocked-state replacement, local reconciliation with restart-repairable business Audit, and the approved reply operation now compose with the exclusive Host lease under synthetic contracts. The explicit callback, reauthorization, and reconciliation product entries are also covered synthetically, and the pinned Harness model-Draft runtime now composes with restart-repairable local persistence under a keyless deterministic provider. A product model-Draft entry with production provider configuration, interactive Draft editing and exact approval UI, production polling lifecycle, and a real authorized Feishu send remain required. TD-209 and Roadmap Stage 2 stay open until this path passes; synthetic Fetch, loopback, and injected-client evidence must not be presented as a live guarantee. See [Stage 2 Exit Gate](docs/STAGE_2_EXIT_GATE.md).

## Stage 3 Backlog — Jira Context

- [ ] **TD-300 — Add Jira OAuth support and development-only API Token support.**
- [ ] **TD-301 — Incrementally synchronize user-relevant Issues and Comments.**
- [ ] **TD-302 — Implement a read-only JQL search Tool.**
- [ ] **TD-303 — Associate Jira Issues with Feishu Threads and TwinDesk Work Items.**
- [ ] **TD-304 — Surface incomplete Jira context without blocking Feishu drafts.**
- [ ] **TD-305 — Keep Jira writes disabled or feature-flagged until a later stage.**
- [ ] **TD-306 — Pass the Stage 3 exit gate:** drafts can cite verifiable Jira facts and degrade safely.

## Stage 4 Backlog — Personas and Specialist Subagents

- [ ] **TD-400 — Build the Persona editor.**
- [ ] **TD-401 — Visualize layered Skill selection and overrides.**
- [ ] **TD-402 — Configure Tool and data scopes independently from Persona identity.**
- [ ] **TD-403 — Configure autonomy, time, token, Tool-call, and delegation budgets.**
- [ ] **TD-404 — Productize Codex as a code and repository specialist.**
- [ ] **TD-405 — Add one-shot Drafter and Critic Subagents.**
- [ ] **TD-406 — Compare single-Agent and delegated run cost, latency, and outcomes.**
- [ ] **TD-407 — Pass the Stage 4 exit gate:** users can predict each Persona's identity, capability, and authority boundaries.

## Stage 5 Backlog — Teams, Automation, and Desktop Experience

Do not begin this stage before real MVP usage feedback exists.

- [ ] **TD-500 — Define and validate Team Templates.**
- [ ] **TD-501 — Add dynamic Workflows with bounded execution.**
- [ ] **TD-502 — Evaluate experimental Agent Teams outside the critical path.**
- [ ] **TD-503 — Enable approved Jira comment and transition operations.**
- [ ] **TD-504 — Add allowlisted low-risk automation.**
- [ ] **TD-505 — Design an optional stateless Webhook Relay.**
- [ ] **TD-506 — Add a desktop shell, system tray, notifications, and quick actions.**

## Cross-Cutting Gates

Apply these gates to every relevant task:

- [ ] Every persisted feature has a restart recovery test.
- [ ] Every external write has an idempotency and uncertain-result test.
- [ ] Every Connector path tests duplicate and out-of-order events.
- [ ] Every sensitive field has redaction, retention, and export coverage.
- [ ] Every approval path fails closed on rejection, cancellation, missing identity, missing scope, or missing responder.
- [ ] Every Subagent or Team path enforces equal-or-narrower authority and explicit budgets.
- [ ] Every Harness upgrade runs the compatibility smoke suite.
- [ ] Every repository document remains in English.
- [ ] Every pull-request commit contains a matching DCO sign-off.

## Explicitly Deferred

- **Multi-user SaaS and organization administration** — outside the initial product boundary.
- **Plugin marketplace** — premature before a stable plugin contract and real usage.
- **Mobile client** — not required for the first closed loop.
- **Vector database** — deferred until measured retrieval needs exceed SQLite and explicit references.
- **Automatic replies without confirmation** — conflicts with the MVP `draft_only` default.
- **Arbitrary third-party code installation by Agents** — conflicts with the security model.
- **Experimental Agent Teams on the MVP critical path** — deferred until Subagent and Workflow behavior is proven.
